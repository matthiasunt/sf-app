import { BeautifulDatePipe } from './beautiful-date.pipe';

describe('BeautifulDatePipe', () => {
  it('create an instance', () => {
    const pipe = new BeautifulDatePipe();
    expect(pipe).toBeTruthy();
  });
});
