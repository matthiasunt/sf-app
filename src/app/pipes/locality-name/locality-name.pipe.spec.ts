import { LocalityNamePipe } from './locality-name.pipe';

describe('LocalityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
