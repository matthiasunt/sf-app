import { ContrastColorPipe } from './contrast-color.pipe';

describe('ContrastColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrastColorPipe();
    expect(pipe).toBeTruthy();
  });
});
