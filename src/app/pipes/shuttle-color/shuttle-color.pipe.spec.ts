import { ShuttleColorPipe } from './shuttle-color.pipe';

describe('ShuttleColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ShuttleColorPipe();
    expect(pipe).toBeTruthy();
  });
});
