import { DistrictNamePipe } from './district-name.pipe';

describe('DistrictNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DistrictNamePipe();
    expect(pipe).toBeTruthy();
  });
});
