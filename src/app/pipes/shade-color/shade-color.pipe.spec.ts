import { ShadeColorPipe } from './shade-color.pipe';

describe('ShadeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ShadeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
