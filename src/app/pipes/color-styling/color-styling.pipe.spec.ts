import { ColorStylingPipe } from './color-styling.pipe';

describe('ColorStylingPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorStylingPipe();
    expect(pipe).toBeTruthy();
  });
});
