import { ShuttleCalledLatelyPipe } from './shuttle-called-lately.pipe';

describe('ShuttleCalledLatelyPipe', () => {
  it('create an instance', () => {
    const pipe = new ShuttleCalledLatelyPipe();
    expect(pipe).toBeTruthy();
  });
});
